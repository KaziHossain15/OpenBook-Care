// Stub — replace with real implementation when ready.
import type { UserPreferences } from "../models/UserPreferences";

interface Props {
  preferences: UserPreferences;
  className?: string;
}

export function CoverageEligibilityCallouts(_props: Props) {
  return null;
}
