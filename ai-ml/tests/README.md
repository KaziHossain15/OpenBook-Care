# AI/ML Tests

Use this folder for draft tests around the same responsibility boundaries as `src/`.

Planned areas:
- `contracts/` for request/response shape checks
- `domain/` for session/message/disclaimer rules
- `application/` for controller and explanation service behavior
- `infrastructure/` for storage, prompt builder, and gateway behavior
